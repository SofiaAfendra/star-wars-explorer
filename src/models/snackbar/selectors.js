export const isSnackbarOpen = ({ snackbar: { isSnackbarOpen } }) =>
  isSnackbarOpen;

export const snackbarType = ({ snackbar: { snackbarType } }) => snackbarType;

export const snackbarContent = ({ snackbar: { snackbarContent } }) =>
  snackbarContent;
